# Seam-Carving
Seam Carving using OpenCV and C++


Prerequisites:
cmake, opencv

To compile on your own machine
———
cd build/
cmake ../
make


To run
——
In the build folder:
./sc image1 new_width new_height image2

 
